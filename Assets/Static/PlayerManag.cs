using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManag : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int x = Playeer.playerCount;
    }

}
